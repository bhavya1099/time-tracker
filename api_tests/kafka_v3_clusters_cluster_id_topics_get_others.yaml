# ********RoostGPT********

# Test generated by RoostGPT for test artilleryTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_get for http method type GET in artillery framework
# RoostTestHash=5a89adf6e3
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect:
      plugin: expect
      assert:
        - contains: "response.headers"
        - equals: "response.statusCode, 400"
        - equals: "response.statusCode, 401"
        - equals: "response.statusCode, 403"
        - equals: "response.statusCode, 404"
        - equals: "response.statusCode, 500"
        - equals: "response.statusCode, 502"
        - equals: "response.statusCode, 503"
        - equals: "response.statusCode, 504"

scenarios:
  - name: Non-Success API Responses
    flow:
      - get:
          url: "/kafka/v3/clusters/{cluster_id}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - contains: "response.headers"
            - equals: "response.statusCode, 400"
            - equals: "response.statusCode, 401"
            - equals: "response.statusCode, 403"
            - equals: "response.statusCode, 404"
            - equals: "response.statusCode, 500"
            - equals: "response.statusCode, 502"
            - equals: "response.statusCode, 503"
            - equals: "response.statusCode, 504"
      - think: 1
      - post:
          url: "/kafka/v3/clusters/{cluster_id}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - contains: "response.headers"
            - equals: "response.statusCode, 400"
            - equals: "response.statusCode, 401"
            - equals: "response.statusCode, 403"
            - equals: "response.statusCode, 404"
            - equals: "response.statusCode, 500"
            - equals: "response.statusCode, 502"
            - equals: "response.statusCode, 503"
            - equals: "response.statusCode, 504"
      - think: 1
      - post:
          url: "/kafka/v3/clusters/{cluster_id}/topics"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          payload:
            name: "Invalid Topic"
          expect:
            - contains: "response.headers"
            - equals: "response.statusCode, 400"
            - equals: "response.statusCode, 401"
            - equals: "response.statusCode, 403"
            - equals: "response.statusCode, 404"
            - equals: "response.statusCode, 500"
            - equals: "response.statusCode, 502"
            - equals: "response.statusCode, 503"
            - equals: "response.statusCode, 504"
      - think: 1
      - delete:
          url: "/kafka/v3/clusters/{cluster_id}/topics/{topic_id}"
          headers:
            Authorization: "{{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - contains: "response.headers"
            - equals: "response.statusCode, 400"
            - equals: "response.statusCode, 401"
            - equals: "response.statusCode, 403"
            - equals: "response.statusCode, 404"
            - equals: "response.statusCode, 500"
            - equals: "response.statusCode, 502"
            - equals: "response.statusCode, 503"
            - equals: "response.statusCode, 504"
