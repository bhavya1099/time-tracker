# ********RoostGPT********

# Test generated by RoostGPT for test artilleryTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics_get for http method type GET in artillery framework
# RoostTestHash=5a89adf6e3
# 
# 

# ********RoostGPT********
config:
  target: {{ $processEnvironment.API_HOST }}
  plugins:
    expect: {}

scenarios:
  - name: Validate Successful API Responses
    flow:
      - log: 
          - "Sending GET request to /kafka/v3/clusters/{cluster_id}/topics"
      - get:
          url: "/kafka/v3/clusters/{cluster_id}/topics"
          headers:
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 200-299
      - think: 2
      - log: 
          - "GET request to /kafka/v3/clusters/{cluster_id}/topics successful"
      
      - log: 
          - "Sending POST request to /kafka/v3/clusters/{cluster_id}/topics"
      - post:
          url: "/kafka/v3/clusters/{cluster_id}/topics"
          headers:
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          payload:
            # Add payload fields as per API specification
        expect:
          - statusCode: 200-299
      - think: 2
      - log: 
          - "POST request to /kafka/v3/clusters/{cluster_id}/topics successful"
      
      - log: 
          - "Sending PATCH request to /kafka/v3/clusters/{cluster_id}/topics"
      - patch:
          url: "/kafka/v3/clusters/{cluster_id}/topics"
          headers:
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
          payload:
            # Add payload fields as per API specification
        expect:
          - statusCode: 200-299
      - think: 2
      - log: 
          - "PATCH request to /kafka/v3/clusters/{cluster_id}/topics successful"
      
      - log: 
          - "Sending DELETE request to /kafka/v3/clusters/{cluster_id}/topics"
      - delete:
          url: "/kafka/v3/clusters/{cluster_id}/topics"
          headers:
            Authorization: "{{$processEnvironment.AUTH_TOKEN}}"
        expect:
          - statusCode: 200-299
      - think: 2
      - log: 
          - "DELETE request to /kafka/v3/clusters/{cluster_id}/topics successful"
