# ********RoostGPT********

# Test generated by RoostGPT for test artilleryTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_patch for http method type PATCH in artillery framework
# RoostTestHash=9d07b67b96
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: Success API Responses
    flow:
      - log: 
          - "Request 1: Update Partition Count"
      - post:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
            content-type: "application/json"
          payload:
            partitions_count: 1
          expect:
            - statusCode: 200-299
      - think: 1
      - log: 
          - "Request 2: Get Topic Details"
      - get:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200-299
      - think: 1
      - log: 
          - "Request 3: Delete Topic"
      - delete:
          url: "/kafka/v3/clusters/{{ cluster_id }}/topics/{{ topic_name }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200-299