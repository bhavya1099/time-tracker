# ********RoostGPT********

# Test generated by RoostGPT for test artilleryTest using AI Type  and AI Model 
# 
# Test generated for /kafka/v3/clusters/{cluster_id}/topics/{topic_name}_patch for http method type PATCH in artillery framework
# RoostTestHash=9d07b67b96
# 
# 

# ********RoostGPT********
config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}

scenarios:
  - name: Non-Success API Responses
    flow:
      - log: "Starting scenario: Non-Success API Responses"
      - think: 1000
      - log: "Request 1: Update Partition Count"
      - post:
          url: "/kafka/v3/clusters/cluster-1/topics/topic-1"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          payload:
            partitions_count: 0
          expect:
            statusCode: 5XX
            json:
              - "$.error_code"
              - "$.message"
      - think: 1000
      - log: "Request 2: Invalid URL"
      - get:
          url: "/invalid/url"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
          expect:
            statusCode: 4XX
      - think: 1000
      - log: "Request 3: Missing Authorization"
      - post:
          url: "/kafka/v3/clusters/cluster-1/topics/topic-1"
          headers:
            Content-Type: "application/json"
          payload:
            partitions_count: 0
          expect:
            statusCode: 4XX
            json:
              - "$.error_code"
              - "$.message"
      - think: 1000
      - log: "Request 4: Invalid Payload"
      - post:
          url: "/kafka/v3/clusters/cluster-1/topics/topic-1"
          headers:
            Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"
            Content-Type: "application/json"
          payload:
            invalid_field: "invalid_value"
          expect:
            statusCode: 4XX
            json:
              - "$.error_code"
              - "$.message"
